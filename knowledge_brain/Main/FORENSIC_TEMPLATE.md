---
module: main
last_updated: 2026-08-19
---

# Main — Forensic Template

| Symptom | Check first | Likely suspects |
|---|---|---|
| Tapping Jewellery in the bottom nav does nothing / navigates wrong | Confirm `index == 4` branch in `_onTabTapped` (`main_screen.dart:76-79`) still does `Navigator.of(context).pushNamed('/jewellery')` and returns **before** touching `selectedTabProvider`. | A refactor that accidentally routed index 4 through the same `switch` as 0-3 (there is no `case 4:` in the switch today — it would silently fall through and do nothing beyond setting the provider). Also check `/jewellery` route registration (`app_router.dart:376`) is intact. |
| Home dashboard/portfolio shows stale data after returning from MPIN verify or payment | Check `initState`'s post-frame callback actually ran — verify `rehydrateFromStorage()` didn't throw (would leave later invalidations un-run since they're sequential `await`s in the same callback). | An exception inside `authControllerProvider.notifier.rehydrateFromStorage()` aborting the rest of `initState`'s callback silently (no try/catch wraps this block, `main_screen.dart:41-65`). |
| Notification badge count shows 0 or stale right after login | Check ordering: `refreshUnreadCount()` is called **after** `rehydrateFromStorage()` completes — if a race exists between `HomeScreen.initState` (which may also read notification state) and this call, the badge may lag. | Confirm `HomeScreen`'s own init logic isn't independently racing this call in a way that overwrites it — see inline comment at `main_screen.dart:61-64`. |
| Transaction history tab shows duplicate/doubled initial fetch | Check `_visitedTabs.contains(2)` guard at `main_screen.dart:106` — if this condition is ever removed or inverted, both `TransactionHistoryScreen.build()`'s own initial `ref.watch` fetch AND `_onTabTapped`'s `.refresh()` would fire on first visit. | Regression to the guard condition, or `TransactionHistoryScreen` itself changed to no longer auto-fetch on first `ref.watch` (would make the guard's assumption stale). |
| History tab loses an active filter after tab-switching back to it | Confirm `_onTabTapped` case 2 still uses `historyProvider.notifier.refresh()`, **not** `ref.invalidate(historyProvider)` — invalidate recreates the notifier from scratch and drops any filter state. | Someone "simplified" `.refresh()` to `ref.invalidate(...)` without reading the inline comment explaining why refresh was chosen (`main_screen.dart:96-105`). |
| App exits on a single back-press from the Home tab (no toast, no second-press grace) | Check `_lastBackPressTime` comparison logic and that `AppToast.show` isn't throwing (would abort the `else` branch silently if unguarded). | Regression in the `isSecondPress` boolean at `main_screen.dart:140-141`, or the 2-second `Duration` constant changed. |
| Bottom nav bar missing on a tab where it should be visible | Check `keyboardOpen` computation (`MediaQuery.viewInsets.bottom > 0`) — a lingering non-zero inset (e.g. after a bottom sheet closes without fully resetting insets) could keep the nav hidden. Also confirm `selectedIndex != 1` if the Invest-tab-hides-nav rule is not intended here. | `resizeToAvoidBottomInset: false` on the `Scaffold` (`main_screen.dart:157`) combined with a lingering `viewInsets` value from another dismissed input surface. |
| A tab's screen re-runs its expensive init logic every time you switch back to it | This should NOT happen for tabs 0-3 — `IndexedStack` + `_visitedTabs` are specifically designed to preserve state across tab switches. If it's happening, the screen itself is likely rebuilding via a `key` change or an external `ref.invalidate` outside this file. | Check the affected screen's own `STATE_ANALYSIS.md`/`MODULE_BRAIN.md` (e.g. `Home`, `Profile`) once built — the bug is more likely in that screen than in `main_screen.dart`'s hosting logic. |
